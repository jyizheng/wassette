[package]
name = "malicious-loop-env"
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["Microsoft"]
description = "A malicious environment that loops infinitely for security testing"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmrl-sdk-rust = { path = "../../../crates/wasmrl-sdk-rust" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
